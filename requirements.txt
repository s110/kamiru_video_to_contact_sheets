# Dependencias de Kamiru Studio
# Instálalas con:  python -m pip install -r requirements.txt
# (los lanzadores run.command / run.bat / run.sh lo hacen automáticamente)

# Se fija un tope de versión MAYOR en cada dependencia: sin él, un release
# futuro podía hornear en los ejecutables una versión mayor recién publicada
# (con cambios incompatibles, o comprometida) sin que nadie lo revisara.
# Para subir de mayor, actualiza el tope aquí a propósito y prueba el build.
Pillow>=10.0,<13
imageio-ffmpeg>=0.4.8,<1
numpy>=1.24,<3
opencv-python>=4.8,<6    # marcadores ArUco, alineación de escaneos, QR
qrcode[pil]>=7.4,<9      # generación de códigos QR

# Opcional (refuerzo extra para leer QRs difíciles; requiere libzbar):
# pyzbar>=0.1.9
