# ARCHIVO GENERADO — no lo edites a mano.
# Las dependencias reales se declaran en pyproject.toml y se fijan en uv.lock.
# Regenéralo con:
#     uv export --frozen --extra desktop --no-hashes --no-emit-project -o requirements.txt
#
# Solo es el plan B para quien no tenga uv:
#     python -m pip install -r requirements.txt
# Lo recomendado es:  uv sync --extra desktop
#
# Opcional (refuerzo extra para leer QRs difíciles; requiere libzbar):
#     pyzbar>=0.1.9
colorama==0.4.6 ; sys_platform == 'win32'
    # via qrcode
imageio-ffmpeg==0.6.0
    # via kamiru-studio
numpy==2.2.6 ; python_full_version < '3.11'
    # via
    #   kamiru-studio
    #   opencv-python
numpy==2.4.6 ; python_full_version == '3.11.*'
    # via
    #   kamiru-studio
    #   opencv-python
numpy==2.5.1 ; python_full_version >= '3.12'
    # via
    #   kamiru-studio
    #   opencv-python
opencv-python==5.0.0.93
    # via kamiru-studio
pillow==12.3.0
    # via
    #   kamiru-studio
    #   qrcode
qrcode==8.2
    # via kamiru-studio
